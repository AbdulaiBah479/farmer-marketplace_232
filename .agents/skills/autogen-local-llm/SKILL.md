# AutoGen with Local Ollama Models
Build multi-agent AI systems with Microsoft AutoGen and Ollama. Covers `AssistantAgent` + `UserProxyAgent` configuration with local LLMs, group chat orchestration, code execution agents, and custom termination conditions.
