# BERT Local Text Classification
Run BERT and RoBERTa models locally for text classification, NER, and sentiment analysis. Covers HuggingFace pipeline, fine-tuning on custom data, ONNX export for fast inference, and batch processing large datasets.
