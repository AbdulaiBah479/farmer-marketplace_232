# Continue.dev with Local LLMs via Ollama
Configure the Continue IDE extension with Ollama for free local AI coding assistance. Covers model config in `config.json`, tab completion with `qwen2.5-coder`, chat with `deepseek-coder-v2`, and context providers.
