# CSS Keyframe Animation
Create smooth CSS animations with @keyframes. Covers animation properties (duration, timing-function, delay, iteration-count, direction, fill-mode), easing curves, composite animations, and performance with will-change and transform.
