# Figma Components and Variants
Build scalable design systems with Figma components. Covers main/instance hierarchy, component properties (text, boolean, instance swap), variant creation, nested components, interactive component states, and publishing to team library.
