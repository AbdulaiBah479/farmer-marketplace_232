# GPT4All Local Models
Run GPT4All for fully private local AI on CPU/GPU. Covers model download, LocalDocs for document Q&A, Python bindings, API server mode, and running without internet on air-gapped machines.
