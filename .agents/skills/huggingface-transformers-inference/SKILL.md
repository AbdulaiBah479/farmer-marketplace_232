# Hugging Face Transformers Inference
Run any HuggingFace model locally with the `transformers` library. Covers `pipeline()` for text generation, chat templates, quantization with `bitsandbytes`, device mapping, and `AutoModelForCausalLM`.
