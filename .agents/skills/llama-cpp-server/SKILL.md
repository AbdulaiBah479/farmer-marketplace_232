# llama.cpp HTTP Server
Run llama.cpp as an HTTP inference server. Covers starting `llama-server`, OpenAI-compatible endpoints, slot management for concurrent requests, built-in web chat UI, and health monitoring.
