# LlamaIndex Local RAG with Ollama
Build private RAG (Retrieval-Augmented Generation) pipelines using LlamaIndex + Ollama. Covers document indexing, local embeddings with `nomic-embed-text`, vector store setup, and query engines that run entirely offline.
