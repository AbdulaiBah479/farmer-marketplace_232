# llama-cpp-python Bindings
Use `llama-cpp-python` for direct Python access to llama.cpp. Covers GGUF model loading, `Llama` class API, chat completion format, grammar-constrained generation, embeddings, and async server mode.
