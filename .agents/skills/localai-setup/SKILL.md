# LocalAI Setup and Configuration
Install and configure LocalAI — the free OpenAI-compatible local inference server. Covers Docker setup, model YAML configuration, backend selection (llama.cpp, whisper, stable-diffusion), and GPU support.
