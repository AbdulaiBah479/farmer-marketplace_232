# MCP Docker Container Management
Use the Docker MCP to manage containers from Claude. Covers container lifecycle (run/stop/restart), image management, Docker Compose operations, volume management, network configuration, and container log access.
