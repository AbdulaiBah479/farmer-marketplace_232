# MCP MongoDB NoSQL Database
Use the MongoDB MCP to interact with MongoDB from Claude. Covers collection queries, aggregation pipelines, index management, schema inspection, CRUD operations, Atlas search, and database administration tasks.
