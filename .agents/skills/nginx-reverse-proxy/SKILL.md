# Nginx Reverse Proxy and Web Server
Configure Nginx as a reverse proxy and web server. Covers server blocks (virtual hosts), upstream proxy_pass, load balancing, SSL termination with Certbot, caching, rate limiting, gzip compression, and security headers configuration.
