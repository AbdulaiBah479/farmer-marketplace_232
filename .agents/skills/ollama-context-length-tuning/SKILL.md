# Ollama Context Length Tuning
Extend context windows in Ollama models. Covers `num_ctx` parameter configuration, Modelfile PARAMETER settings, memory requirements per context size, and using rope_freq_base/scale for context extension.
