# Ollama Docker Deployment
Deploy Ollama in Docker containers. Covers `docker run ollama/ollama`, GPU passthrough with `--gpus all`, volume mounts for model persistence, and Docker Compose setups for local AI stacks.
