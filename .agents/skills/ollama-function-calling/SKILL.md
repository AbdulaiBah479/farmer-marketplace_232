# Ollama Function Calling and Tool Use
Implement function calling with Ollama models that support tools. Covers JSON schema tool definitions, `tools` parameter in chat API, parsing tool call responses, and building agents with local function execution.
