# Ollama Health Monitoring and Alerting
Monitor Ollama server health in production. Covers `/api/version` health checks, Prometheus metrics scraping, Grafana dashboard setup, alerting on model load failures, GPU temperature monitoring, and auto-restart scripts.
