# Ollama + LangChain RAG Pipeline
Build a complete RAG system with Ollama and LangChain. Covers document loading, text splitting, embedding with `nomic-embed-text`, Chroma vector store, retrieval chain, and conversational Q&A over private documents.
