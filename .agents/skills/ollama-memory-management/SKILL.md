# Ollama Memory Management
Manage GPU and CPU memory for Ollama models. Covers `OLLAMA_KEEP_ALIVE` to keep models loaded, model unloading strategies, checking GPU utilization with `nvidia-smi`, and memory mapping for large models.
