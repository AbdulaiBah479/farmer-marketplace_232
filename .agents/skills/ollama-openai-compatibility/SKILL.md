# Ollama OpenAI API Compatibility
Use Ollama as an OpenAI-compatible API endpoint. Point existing OpenAI SDK code to `http://localhost:11434/v1` to run local models without code changes using the same chat/completion interface.
