# Ollama Proxy and Gateway Server
Build a proxy gateway for Ollama that adds authentication, logging, and routing. Covers LiteLLM proxy configuration, model aliasing, OpenAI-compatible routing to Ollama, rate limiting, and usage tracking.
