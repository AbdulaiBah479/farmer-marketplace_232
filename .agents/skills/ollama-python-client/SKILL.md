# Ollama Python Client
Use the official `ollama` Python library for local LLM inference. Covers `ollama.chat()`, `ollama.generate()`, streaming responses, async calls, and embedding generation for Python applications.
