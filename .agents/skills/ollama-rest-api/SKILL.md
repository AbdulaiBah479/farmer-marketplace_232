# Ollama REST API Integration
Call the Ollama REST API (`/api/generate`, `/api/chat`, `/api/embeddings`) from any language. Covers authentication-free local inference, streaming responses, and JSON request formatting.
