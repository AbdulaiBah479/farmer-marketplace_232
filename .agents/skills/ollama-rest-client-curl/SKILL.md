# Ollama REST API with cURL
Query Ollama models using cURL for shell scripting and quick testing. Covers `/api/generate` streaming, `/api/chat` multi-turn, `/api/embeddings`, model pull/push endpoints, and parsing JSON responses with `jq`.
