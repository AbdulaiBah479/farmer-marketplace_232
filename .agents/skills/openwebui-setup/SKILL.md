# Open WebUI Setup
Install and configure Open WebUI — the ChatGPT-like frontend for Ollama and OpenAI-compatible APIs. Covers Docker deployment, user management, model selection, system prompt configuration, and RAG setup.
