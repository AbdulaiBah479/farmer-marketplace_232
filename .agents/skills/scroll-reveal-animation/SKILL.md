# Scroll Reveal and Intersection Observer Animations
Trigger animations as elements enter the viewport. Covers Intersection Observer API, ScrollReveal.js, AOS (Animate on Scroll), fade-in/slide-in patterns, lazy loading with animation, and accessibility with prefers-reduced-motion.
