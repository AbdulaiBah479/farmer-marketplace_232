# Streamlit Ollama Application
Build interactive local AI apps with Streamlit and Ollama. Covers `st.chat_message`, streaming responses with `st.write_stream`, session state for conversation history, sidebar model selection, and caching embeddings.
