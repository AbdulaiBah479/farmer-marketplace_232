# Vite Build Tooling and Configuration
Configure Vite for fast development and optimized production builds. Covers plugin ecosystem, path aliases, environment variables, proxy configuration, library mode for packages, CSS modules, dynamic imports, and migration from webpack/CRA.
