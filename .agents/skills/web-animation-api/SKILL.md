# Web Animations API (WAAPI)
Use the native Web Animations API for performant JavaScript animations. Covers `element.animate()`, KeyframeEffect, Animation controls (play/pause/reverse), composite operations, and replacing Greensock with native APIs.
