# Web Security and OWASP Top 10
Secure web applications against common vulnerabilities. Covers XSS prevention with CSP, SQL injection with parameterized queries, CSRF tokens, secure session management, dependency vulnerability scanning, CORS configuration, and security headers (Helmet.js).
